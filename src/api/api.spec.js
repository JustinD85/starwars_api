import API from './api';

describe('API', () => {
  let type, options;
  beforeEach(() => {
    type = 'planets'
    options = ''
  })
  it('should call fetch with correct params', () => {
    
  })

  it('should call feth with correct optional params', () => {
    
  })

  it('should return starwars items given normal params', () => {
    
  })

  it('should return starwars item given options params', () => {
    
  })

  it('should throw an error if something is wrong', () => {
    
  })
})